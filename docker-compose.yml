version: '3.8'

services:
  # frontend:
  #   build:
  #     context: ./frontend 
  #   ports:
  #     - "3000:3000"
  #   depends_on:
  #     - backend

  backend:
    build:
      context: ./backend 
      dockerfile: Dockerfile  
    ports:
      - "4000:4000"  
    volumes:
      - ./backend/uploads:/usr/src/app/uploads